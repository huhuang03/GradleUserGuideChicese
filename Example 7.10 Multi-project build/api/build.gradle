dependencies {
    compile project(':shared')
}

task dist(type: zip) {
    dependsOn spiJar
    from "src/dist"
    into('libs') {
        from spiJar.archivePath
        from configurations.runtime
    } 
}

artifacts {
    archives dist
}
